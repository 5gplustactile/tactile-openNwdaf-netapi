apiVersion: v1
kind: Service
metadata:
  name: {{ include "nwdaf-netapi.fullname" . }}
  labels:
    {{- include "nwdaf-netapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nwdaf-netapi.selectorLabels" . | nindent 4 }}
